{
	"name": "gamestuff/gamestuff-blocks",
	"description": "Development tooling for the GameStuff Blocks plugin. Used only for code quality checks (WPCS) — the plugin itself does not depend on vendor/autoload.php at runtime; see includes/Core class loading, wired up via a custom spl_autoload_register() in gamestuff-blocks.php.",
	"type": "wordpress-plugin",
	"license": "GPL-2.0-or-later",
	"authors": [
		{
			"name": "Irvan Noerfazri"
		}
	],
	"require": {
		"php": ">=8.0"
	},
	"require-dev": {
		"squizlabs/php_codesniffer": "^3.9",
		"wp-coding-standards/wpcs": "^3.1",
		"phpcompatibility/phpcompatibility-wp": "^2.1",
		"dealerdirect/phpcodesniffer-composer-installer": "^1.0"
	},
	"config": {
		"allow-plugins": {
			"dealerdirect/phpcodesniffer-composer-installer": true
		}
	},
	"scripts": {
		"lint": "phpcs",
		"lint:fix": "phpcbf"
	},
	"minimum-stability": "stable",
	"prefer-stable": true
}
